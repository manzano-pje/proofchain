import { Link } from "react-router-dom";
import { motion } from "framer-motion";
import { Shield, FileCheck, Globe } from "lucide-react";
export default function Landing() {
  return (<main className="min-h-screen flex flex-col"><nav className="px-6 py-4 flex justify-between items-center max-w-7xl mx-auto w-full"><span className="text-xl font-bold text-primary">🔷 Proofchain</span><Link to="/validate" className="btn-secondary">Validar Agora</Link></nav><section className="flex-1 flex flex-col justify-center items-center px-6 text-center max-w-4xl mx-auto"><motion.h1 initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="text-4xl md:text-6xl font-bold text-dark leading-tight">Valide certificados digitais de forma <span className="text-primary">pública e segura</span></motion.h1><motion.p initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.2 }} className="mt-6 text-xl text-gray-600">Garanta autenticidade com registros criptográficos verificáveis. Sem intermediários, sem falsificações.</motion.p><motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.4 }} className="mt-10 flex flex-col sm:flex-row gap-4"><Link to="/validate" className="btn-primary">🔍 Validar Certificado</Link><Link to="/login" className="btn-secondary">📄 Acessar Plataforma</Link></motion.div><div className="mt-20 grid grid-cols-1 md:grid-cols-3 gap-8 w-full"><Feature icon={<Shield size={28}/>} title="Prova Criptográfica" desc="Hash imutável registrado publicamente"/><Feature icon={<FileCheck size={28}/>} title="Emissão Rápida" desc="Gere e assine em segundos"/><Feature icon={<Globe size={28}/>} title="Validação Pública" desc="Qualquer pessoa verifica, em qualquer lugar"/></div></section></main>);
}
const Feature = ({ icon, title, desc }: any) => (<motion.div whileHover={{ y: -4 }} className="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 text-left"><div className="w-12 h-12 bg-primary/10 text-primary rounded-xl flex items-center justify-center mb-4">{icon}</div><h3 className="font-bold text-lg mb-2">{title}</h3><p className="text-gray-600 text-sm">{desc}</p></motion.div>);